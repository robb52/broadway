<h1 class="curent-category"><%= params[:category] %></h1>

<% if @plays.count == 0 %>
  <h3>There are no plays</h3>
<% else %>
  <% @plays.each do |play| %>
    <h2><%= link_to play.title, play_path(play) %></h2>
  <% end %>
<% end %>

